# Preferred: an HTTPS endpoint that accepts the contact form as JSON and sends the email server-side.
VITE_CONTACT_FORM_ENDPOINT=

# Optional fallback: opens the visitor's email application with the form details prepared.
VITE_CONTACT_EMAIL=
